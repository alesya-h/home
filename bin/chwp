#!/bin/bash
# --bg-fill is the mode you want. --bg-max is one with black margins.
if [ -z "$1" ]; then
  MODE="--bg-fill"
else
  MODE="--bg-max"
fi
find ~/Pictures/wallpapers/current|sort --random-sort|head -2|xargs --delimiter=\\n feh "$MODE"
