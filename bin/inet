#!/bin/bash
if [ "$DISPLAY" ]; then
    export SUDO_ASKPASS=/usr/lib/ssh/ssh-askpass
    SUDO='sudo -A'
else
    SUDO='sudo'
fi
cat <<EOF|$SUDO bash
(
netctl start ethernet-dhcp
sleep $((${1-5}*60))
netctl stop ethernet-dhcp
)&
EOF
